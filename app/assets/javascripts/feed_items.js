$(function() {
  var pageNumber = 0

  // $(window).scroll(function() {
    // if($(window).scrollTop() + $(window).height() > $(document).height() - 100) {
      // $.get('/dashboard?page=' + pageNumber, function(data) {
        // debugger;
      // });
    // }
  // });

});
